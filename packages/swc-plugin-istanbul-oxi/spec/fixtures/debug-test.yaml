# For easier single test fixture debugging, `npm run test:debug` runs this test only.
# Place any test want to run.
---
name: ignore file comment
code: |
  /* istanbul ignore file */
  output = true === true ? "works" : "doesn't work"
opts:
  noCoverage: true
tests:
  - name: file is ignored
